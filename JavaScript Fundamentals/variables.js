// we can use let for dynamic values, it can be updated later 
let age = 23;

// we use const for constant values through out the program 
const k = 3;
console.log(age);
console.log('my age is ' + age)
console.log('this is the value of k' + k)